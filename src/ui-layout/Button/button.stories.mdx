<!-- TestComponent.stories.mdx-->

import { Meta, Story } from '@storybook/addon-docs'

import { Button } from './index'
import { Add } from '@mui/icons-material'

<Meta title="Button" component={Button} />

<!-- 👇 We create a “template” of how args map to rendering -->

export const Template = args => <Button {...args} />

<!-- 👇 Each story then reuses that template -->

<Story
  name="PrimaryOutlined"
  args={{
    color: 'primary',
    variant: 'outlined',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="PrimaryContained"
  args={{
    color: 'primary',
    variant: 'contained',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="SecondaryOutlined"
  args={{
    color: 'secondary',
    variant: 'outlined',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="SecondaryContained"
  args={{
    color: 'secondary',
    variant: 'contained',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="PrimaryText"
  args={{
    color: 'primary',
    variant: 'text',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="SecondaryText"
  args={{
    color: 'secondary',
    variant: 'text',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="PrimarySoft"
  args={{
    color: 'primary',
    variant: 'soft',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="SecondarySoft"
  args={{
    color: 'secondary',
    variant: 'soft',
    children: 'Botão',
  }}
>
  {Template.bind({})}
</Story>

<Story
  name="Icon Button"
  args={{
    color: 'primary',
    variant: 'outlined',
    children: 'Adicionar',
    startIcon: <Add />,
  }}
>
  {Template.bind({})}
</Story>
